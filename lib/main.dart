import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      initialRoute: '/',
      routes: {
        // '/': (context) => AlarmSetupScreen(),
        // '/countdown': (context) => CountdownScreen(),
        //'/game': (context) => SimonGameScreen(),
        //'/success': (context) => SuccessScreen(),
      },
    ),
  );
}
